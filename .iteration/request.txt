用户新增 `ridgecode goal`，要求用 NotebookLM 深度调研长时间任务如何持久执行、收敛并完成目标；随后要求回到本机 MCP/tool 查询配置与修复链路问题。

批准

修复 TUI 交互页面不换行导致展示不全问题，并整体重构为科技感、高级感终端界面；模型回复、调查结论、等待状态须可见，支持展示部分内容并展开查看详情，修复 Ctrl+R / Ctrl+O；底部与状态条 token 统计须区分输入 token 与输出 token。

直接通过，无需另行审批。

忙碌时 pending 消息须粘性显示于输入框上方；提供立即推入队首能力（Ctrl+Enter），普通 Enter 仍追加队尾。
本轮将 TUI UI 纳入迭代，改为更优雅、美观、深邃、克制的设计；当前颜色过于花哨，需参考 NLM 中的 TUI 设计案例并向 Claude Code 的信息层级与交互质感对齐。
补充 TUI 交互要求：输出内容必须按终端宽度换行；文件读取/工具输出默认折叠，保留摘要并可展开；实时展示 Agent 当前阶段、调查结论、等待、工具调用与可接管状态，允许用户随时干预/接管；发送消息既可排队，也须提供快捷键直接插入当前队列而不打断模型思考。
