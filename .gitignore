/target
**/*.rs.bk
.DS_Store
# Windows 下 Git Bash 崩溃残留
*.stackdump

# 本地密钥 / 环境变量(绝不入库)
.env
.env.local
.env.*

# CodeGraph 索引(本地生成,不入库)
/.codegraph/

# SDD 进度 ledger(本地 scratch,不入库)
/.superpowers/

# eval fixture seed 的构建产物(不入库;根 Cargo.lock 仍跟踪)
**/target/
crates/rc-eval/tasks/*/seed/Cargo.lock
