/target
**/*.rs.bk
.DS_Store
# Windows 下 Git Bash 崩溃残留
*.stackdump
# 本地打包产物(二进制不入库;跨平台发布走 release.yml)
/dist/

# 本地密钥 / 环境变量(绝不入库)
.env
.env.local
.env.*

# CodeGraph 索引(本地生成,不入库)
/.codegraph/

# SDD 进度 ledger(本地 scratch,不入库)
/.superpowers/

# 各 crate 的构建产物(不入库;根 Cargo.lock 仍跟踪)
**/target/

# 沙箱测试临时产物(写 jail 后测试落在 cwd 子树,自清理;防 panic 残留污染)
*.tmp
ridge_agent_batch_tmp/
