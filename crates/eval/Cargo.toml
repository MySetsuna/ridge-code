[package]
name = "eval"
description = "eval harness:批量跑 agent,量成功率 + 成本(verification-first 的质量度量)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[[bin]]
name = "ridge-eval"
path = "src/main.rs"

[dependencies]
agent = { path = "../agent" }
provider = { path = "../provider" }
tokio = { workspace = true }
anyhow = { workspace = true }
serde_json = { workspace = true }
