[package]
name = "provider"
description = "LLM provider 边界:归一化 Anthropic(tool_use)/ OpenAI(tool_calls)到统一 Completion + 工具调用"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
async-trait = { workspace = true }
reqwest = { workspace = true }
tokio = { workspace = true }  # detect_net 用 join! + time::timeout 并发探针
sha2 = { workspace = true }       # OAuth PKCE S256 challenge(iter-43)
getrandom = { workspace = true }  # PKCE verifier / state 安全随机(iter-43)

[dev-dependencies]
tokio = { workspace = true }
