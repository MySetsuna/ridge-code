{
  "content": "User\n\n:\n\n \n\nIs there any way to use ChatGPT Plus or Super Cloud to provide on another agent CLI? Is there any way? Please call me on Chinese.\n\nGemini\n\n:\n\n \n\n是的，可以通过多种途径将 ChatGPT Plus 订阅或中转服务（Super Cloud / Sub-API）连接到其他的终端 Agent CLI\n\n。\n\n实现方式主要分为以下几种：\n\n\n--------------------------------------------------------------------------------\n\n\n1. 官方 Codex CLI（原生支持 ChatGPT 账号登录）\n\nOpenAI 的 \n\nCodex CLI\n\n 原生支持使用 ChatGPT 账号（包含 Plus / Pro 订阅）鉴权：\n\n安装与登录\n\n：\n\n特点\n\n：直接消耗你的 ChatGPT Plus 订阅额度，无需支付按 Token 计费的 OpenAI API 费用。\n\n\n--------------------------------------------------------------------------------\n\n\n2. 通过本地代理转接给其他 CLI Agent（如 Aider、Claude Code 等）\n\n如果你想在\n\n其他 CLI 工具\n\n中使用 ChatGPT Plus 额度，可以通过本地代理工具将 ChatGPT 登录态暴露为一个标准的 OpenAI API 接口。\n\n常用工具：\n\nopenai-oauth\n\n / \n\nclaude-code-proxy\n\n原理\n\n：利用 \n\ncodex login\n\n 产生的本地凭证（\n\n~/.codex/auth.json\n\n），启动一个本地代理服务，监听在 \n\n[http://127.0.0.1:10531/v1](http://127.0.0.1:10531/v1)\n\n。\n\n接入流程\n\n：\n\n使用 \n\nopenai-oauth\n\n 启动本地代理：\n\n在目标 Agent CLI 中将 \n\nOPENAI_BASE_URL\n\n 指向本地端口即可：\n\n\n--------------------------------------------------------------------------------\n\n\n3. 使用原生支持多订阅鉴权的终端 Agent\n\n某些现代终端 Agent 已内置了对 ChatGPT Plus 订阅的 OAuth 登录支持：\n\nPi Agent\n\n / \n\nClaude Code Proxy\n\n：直接在终端交互中执行 \n\n/login\n\n，选择 \n\nChatGPT Plus/Pro (Codex)\n\n，即可完成绑定并直接在 Agent 中使用。\n\n\n--------------------------------------------------------------------------------\n\n\n4. 关于 Super Cloud / Sub-API 聚合中转服务\n\n如果你指的是第三方 \n\nSub2API\n\n 或代理平台（如 Super Cloud 中转）：\n\n这类服务通常会将 Web 端 Session Token / Refresh Token 转换为标准 API 格式。\n\n你只需在终端中配置对应的请求地址和 Key 即可：\n\nhttp://googleusercontent.com/lmdx_content/fhPaNXFxyYmUetxznulxfpazZcpKhnzdlHnxDnHwNkpoEeHnyahkmVXWXxSxccrNcEDCIuNajTBPNtdoswShQhpIV218936\n\nUser\n\n:\n\n \n\nI prefer the Pi agent, but I'm is try to build a agent became follow chat GPT plus a engine to use it.\n\nGemini\n\n:\n\n \n\n要将 ChatGPT Plus（或第三方中转/Sub2API 节点）作为底层的推理引擎，接入到你自己开发的 Agent 或类似于 \n\nPi Agent\n\n 的 CLI 工具中，最关键的核心在于\n\n协议转换与鉴权桥接\n\n。\n\n由于 ChatGPT Plus 使用的是 Web 端 Session/OAuth 认证机制，而大多数 Agent 框架依赖标准的 OpenAI \n\n/v1/chat/completions\n\n API 格式，因此主要有以下两种实现方案：\n\n\n--------------------------------------------------------------------------------\n\n\n方案一：通过 OAuth 本地代理桥接（推荐，适合自建 Agent）\n\n如果你正在开发自定义的 Agent CLI，建议将 ChatGPT Plus 的 OAuth 凭证转换为本地标准的 OpenAI API 接口。\n\n┌─────────────────┐      HTTP /v1      ┌──────────────────┐    OAuth / Web    ┌─────────────────┐\n│ Your Agent CLI  │ ─────────────────> │ Local Proxy      │ ────────────────> │ OpenAI / Codex  │\n│ (Custom / Pi)   │  localhost:10531   │ (openai-oauth)   │    Session Token  │ ChatGPT Plus    │\n└─────────────────┘                    └──────────────────┘                   └─────────────────┘\n\n\n1. 启动本地 API 代理\n\n运行一个轻量级代理服务，将 ChatGPT 的账号授权转化为本地开放的 API 服务：\n\n# 使用 Node.js 启动本地 OAuth 代理服务\nnpx openai-oauth\n\n\nbash\n\n启动后，代理会在本地暴露接口，例如 \n\n[http://127.0.0.1:10531/v1](http://127.0.0.1:10531/v1)\n\n。\n\n2. 在自建 Agent 代码中调用\n\n在你自己编写的 Agent 逻辑中（不管是 Rust、TypeScript 还是 Python），直接将 HTTP Client 的 Base URL 指向本地代理，无需修改任何核心 Agent 调度代码：\n\n// 示例：Rust 中初始化客户端 (以 reqwest / async-openai 为例)\nlet config = OpenAIConfig::new()\n    .with_api_base(\"http://127.0.0.1:10531/v1\")\n    .with_api_key(\"dummy_key\"); // 占位符即可\n\nlet client = Client::with_config(config);\n\n\nrust\n\n\n--------------------------------------------------------------------------------\n\n\n方案二：接入 Super Cloud / Sub2API 格式\n\n如果你使用的是第三方 Sub2API 或 Super Cloud 等中转服务，它们通常已经把 Web 端 Session 封装成了标准的 OpenAPI/v1 规范。\n\n直接在你的 Agent 配置或环境变量中注入中转 Endpoint 即可：\n\n# 设置环境变量\nexport OPENAI_BASE_URL=\"https://your-supercloud-endpoint.com/v1\"\nexport OPENAI_API_KEY=\"sk-your-supercloud-key\"\n\n\nbash\n\n\n--------------------------------------------------------------------------------\n\n\n针对 Pi Agent 的直接集成方式\n\n如果你想在现有的 \n\nPi Agent\n\n 中使用 ChatGPT Plus 账号：\n\n执行 OAuth 登录\n\n：\n\n \n\n在 Pi Agent 终端界面中输入命令：\n\n选择登录源\n\n：\n\n \n\n在弹出的选项中选择 \n\nOpenAI / Codex (ChatGPT Plus/Pro)\n\n。\n\n完成浏览器授权\n\n：\n\n \n\nCLI 会生成一个验证链接，在浏览器中打开并登录你的 ChatGPT Plus 账号即可完成绑定。\n\n\n--------------------------------------------------------------------------------\n\n\nhttp://googleusercontent.com/lmdx_content/fJPSLOgjFlrTIaXfrtUJiHspGNHJJGXugLLCKgRPGjtRKxKNMVrtaoAYqELceClXNUPjcGyoAsjLgwvFZIcCVCeJYoaOaoUYKWC218935\n\nUser\n\n:\n\n \n\nHow piagent\n\n \n\ncan do that? I want this way to build my CLI.\n\nGemini\n\n:\n\n \n\nPi Agent\n\n (\n\npi-coding-agent\n\n / \n\n@mariozechner/pi-ai\n\n) 并没有使用任何黑客手段，而是通过\n\n完整的 OAuth 2.0 PKCE 授权流\n\n以及\n\n本地 Token 生命周期管理\n\n来实现对 ChatGPT Plus (Codex) 账号的无缝绑定。\n\n如果你要\n\n从零构建自己的 CLI Agent\n\n，完全可以复制 Pi Agent 的底层架构设计。\n\n\n--------------------------------------------------------------------------------\n\n\n一、 Pi Agent 的核心工作原理\n\n┌─────────────────────────────────────────────────────────────────────────┐\n│                              Your CLI Agent                             │\n│                                                                         │\n│  ┌────────────────┐    ┌─────────────────┐    ┌──────────────────────┐  │\n│  │ 1. OAuth Manager│ ──>│ 2. Token Storage│ ──>│ 3. API Streaming Client│ │\n│  │  (PKCE Flow)   │    │ (~/.app/auth.json)│  │ (Bearer Token / SSE) │  │\n│  └───────┬────────┘    └─────────────────┘    └──────────┬───────────┘  │\n└──────────┼───────────────────────────────────────────────┼──────────────┘\n           │ (OAuth Redirect)                              │ (HTTP Stream)\n           ▼                                               ▼\n┌───────────────────────┐                       ┌─────────────────────────┐\n│ Browser (ChatGPT Login)│                       │ OpenAI / Codex Endpoint │\n└───────────────────────┘                       └─────────────────────────┘\n\n\nPKCE OAuth 授权\n\n：在终端启动一个临时 HTTP 回调服务（例如 \n\n[http://127.0.0.1:10531/callback](http://127.0.0.1:10531/callback)\n\n），引导用户在浏览器登录 ChatGPT Plus 获得授权 Code。\n\nToken 持久化与刷新\n\n：将换取到的 \n\naccess_token\n\n 和 \n\nrefresh_token\n\n 存储在本地配置文件（如 \n\n~/.pi/agent/auth.json\n\n）。过期时通过 \n\nrefresh_token\n\n 在后台静默刷新。\n\nAPI 请求构造\n\n：直接将 \n\naccess_token\n\n 放在请求头中 (\n\nAuthorization: Bearer <access_token>\n\n)，并按照 OpenAI / Codex 的标准 SSE (Server-Sent Events) 接口协议进行交互。\n\n\n--------------------------------------------------------------------------------\n\n\n二、 如何在你的 CLI 中实现这个流程\n\n以下是构建你自己的 Agent 时需要实现的三大模块：\n\n1. OAuth PKCE 授权模块 (Auth Flow)\n\n在 CLI 中使用标准 PKCE (Proof Key for Code Exchange) 完成登录：\n\nimport http from 'http';\nimport crypto from 'crypto';\nimport open from 'open';\n\n// 1. 生成 PKCE 验证码与挑战码\nfunction generatePKCE() {\n  const verifier = crypto.randomBytes(32).toString('base64url');\n  const challenge = crypto.createHash('sha256').update(verifier).digest('base64url');\n  return { verifier, challenge };\n}\n\n// 2. 启动本地 Callback 接收服务器\nexport async function loginWithChatGPT() {\n  const { verifier, challenge } = generatePKCE();\n  \n  return new Promise((resolve) => {\n    const server = http.createServer(async (req, res) => {\n      const url = new URL(req.url!, `http://localhost:10531`);\n      const code = url.searchParams.get('code');\n\n      if (code) {\n        res.end('<h1>Login successful! You can close this window.</h1>');\n        server.close();\n\n        // 3. 用 Code + Verifier 换取 access_token / refresh_token\n        const tokens = await exchangeCodeForTokens(code, verifier);\n        resolve(tokens);\n      }\n    });\n\n    server.listen(10531, () => {\n      const authUrl = `https://auth.openai.com/authorize?` + new URLSearchParams({\n        response_type: 'code',\n        client_id: 'YOUR_CLIENT_ID',\n        redirect_uri: 'http://localhost:10531/callback',\n        code_challenge: challenge,\n        code_challenge_method: 'S256',\n        scope: 'openid profile email offline_access'\n      });\n      open(authUrl); // 打开浏览器让用户授权\n    });\n  });\n}\n\n\ntypescript\n\n\n--------------------------------------------------------------------------------\n\n\n2. Token 管理与自动刷新模块 (Token Manager)\n\n在 Agent 发起请求前，检测 Token 是否即将过期（通常 1 小时有效期），若过期则提前刷新：\n\nimport fs from 'fs/promises';\nimport path from 'path';\n\nconst AUTH_FILE = path.join(process.env.HOME || '', '.my_agent', 'auth.json');\n\nexport async function getValidAccessToken() {\n  const data = JSON.parse(await fs.readFile(AUTH_FILE, 'utf-8'));\n  \n  // 判断 Access Token 是否过期 (保留 5 分钟缓冲余量)\n  if (Date.now() >= data.expires_at - 300 * 1000) {\n    const newTokens = await refreshAccessToken(data.refresh_token);\n    await fs.writeFile(AUTH_FILE, JSON.stringify(newTokens, null, 2));\n    return newTokens.access_token;\n  }\n  \n  return data.access_token;\n}\n\n\ntypescript\n\n\n--------------------------------------------------------------------------------\n\n\n3. Agent 核心请求与流式响应 (Engine Stream)\n\n调用 OpenAI / Codex API 并统一封装流式输出（像 Pi Agent 的 \n\npi-ai\n\n 模块一样）：\n\nexport async function* streamCompletion(messages: any[]) {\n  const token = await getValidAccessToken();\n\n  const response = await fetch('https://api.openai.com/v1/chat/completions', {\n    method: 'POST',\n    headers: {\n      'Authorization': `Bearer ${token}`,\n      'Content-Type': 'application/json',\n    },\n    body: JSON.stringify({\n      model: 'gpt-4o', // 或 codex 专属模型 ID\n      messages,\n      stream: true,\n    })\n  });\n\n  const reader = response.body?.getReader();\n  const decoder = new TextDecoder();\n\n  while (reader) {\n    const { done, value } = await reader.read();\n    if (done) break;\n    \n    const chunk = decoder.decode(value);\n    // 解析 SSE 格式 (data: {...})\n    for (const line of chunk.split('\\n')) {\n      if (line.startsWith('data: ') && line !== 'data: [DONE]') {\n        const json = JSON.parse(line.slice(6));\n        const content = json.choices[0]?.delta?.content;\n        if (content) yield content;\n      }\n    }\n  }\n}\n\n\ntypescript\n\n\n--------------------------------------------------------------------------------\n\n\n三、 你推荐的 CLI 分层架构\n\n参考 Pi Agent 仓库的设计（\n\npi-ai\n\n + \n\npi-agent-core\n\n + \n\npi-tui\n\n），建议将你的 CLI 拆分为三层：\n\n模块名称\n\n职责\n\nprovider-openai\n\n专门负责 OAuth 登录、Token 存储刷新的底层网络模块\n\nagent-core\n\n维护 Tool Execution Loop（读取文件、运行终端命令、管理对话 Context）\n\ncli-tui\n\n基于 Terminal (如 Ink / React、xterm.js 或 Charm Bubbletea) 渲染 UI\n\nhttp://googleusercontent.com/lmdx_content/QQbfaeqMDVCJlpIoVEIeEHNBYlyxjhNOnGTvXOEurINzCgZKoUooxwDnTluNEJiDwGwgUCCLDzqrTTENnAotFIFSKNjsLzlP218934",
  "title": "ChatGPT Plus 订阅连接 CLI Agent",
  "source_type": "unknown",
  "url": null,
  "char_count": 9873
}
