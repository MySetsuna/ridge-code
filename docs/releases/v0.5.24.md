## RidgeCode v0.5.24

### 修复

- 修正 Linux/Unix TUI `/doctor` 报告被状态噪声过滤器误吞：噪声判定改按首行，保留多行用户报告中的诊断说明。
- 增加多行诊断文本回归测试，覆盖报告提示含 `verifying` 等状态词的场景。

### 验证

- `cargo fmt --all -- --check`
- `cargo test --workspace --locked --quiet`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
