## RidgeCode v0.5.26

本版聚焦长任务可靠性与动态工具安全边界：

- 工具效果元数据贯通 provider、MCP 与 agent，探索、编辑、验证 handoff 可确定路由。
- 未声明效果的动态工具默认保守处理；仅凭结构化修改事实记录编辑结果。
- 新增长探索评测，验证连续探索后能够触发 handoff、执行编辑并报告修改事实。
- TUI token 流单次有界消费并主动让出调度；输入缓冲限制为 32,768 个 Unicode 字符。

发布前已通过 workspace 测试、Clippy `-D warnings`、格式与差异质量门。
