## RidgeCode v0.5.11

### 其他 PC 快速安装（无需 Rust/Cargo）

Windows PowerShell：

```powershell
irm https://raw.githubusercontent.com/MySetsuna/ridge-code/main/scripts/install.ps1 | iex
```

Linux / macOS：

```bash
curl -fsSL https://raw.githubusercontent.com/MySetsuna/ridge-code/main/scripts/install.sh | sh
```

安装器按当前平台从 GitHub Release 下载独立二进制，先校验 SHA256，再安装到用户 PATH，并创建最小配置骨架。固定版本安装、离线安装、API Key、OAuth、MCP、Goal 与完整 TUI 用法见归档内 `README.md`。

### 可复现安装

Windows：

```powershell
$s = irm https://raw.githubusercontent.com/MySetsuna/ridge-code/v0.5.11/scripts/install.ps1
& ([scriptblock]::Create($s)) -Version v0.5.11
```

Linux / macOS：

```bash
curl -fsSL https://raw.githubusercontent.com/MySetsuna/ridge-code/v0.5.11/scripts/install.sh | sh -s -- --version v0.5.11
```

### 本版内容

- README 的 GitHub 快速安装、固定版本、离线转交与 Release 资产说明统一指向 `v0.5.11`。
- 发布归档继续携带完整 `README.md`、对应平台安装脚本与 `.sha256` 校验文件。
- 二进制版本号与 Release tag 对齐为 `0.5.11`。

### 平台资产

- Windows x86_64：`ridgecode-x86_64-pc-windows-msvc.zip`
- Linux x86_64 / ARM64：`ridgecode-x86_64-unknown-linux-gnu.tar.gz` / `ridgecode-aarch64-unknown-linux-gnu.tar.gz`
- macOS Intel / Apple Silicon：`ridgecode-x86_64-apple-darwin.tar.gz` / `ridgecode-aarch64-apple-darwin.tar.gz`
