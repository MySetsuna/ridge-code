# v0.5.30

- TUI 原生 scrollback 现在按输出块保留单行间隔，连续空行会自动合并。
- 工具输出采用统一的树形连接线；真实回答和工具结果保持可见，推理与工具阶段心跳不再重复写入 scrollback。
- YOLO 及等价跳过权限入口会在当前进程自动启用 cwd 外写入，同时继续保留危险命令、受保护路径和 read-only 硬拦截。
- 增加会话恢复、引导回执、透明终端主题与紧凑 diff 展示改进。
