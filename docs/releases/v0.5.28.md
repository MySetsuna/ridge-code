## RidgeCode v0.5.28

补丁版聚焦长任务迭代后的确定性与文档对齐：

- OAuth profile 注册抽出可注入配置路径的内部 helper，回归测试不再修改进程级 `RIDGE_CONFIG`，避免并行测试竞态。
- SpecTree/Obsidian 导出支持草稿变更节点，并忽略生成的 `.spectree` 审批与证据状态，保持投影与代码实际变更对齐。
- 发布前质量门通过：`cargo test --workspace --locked`、`cargo clippy --workspace --all-targets --locked -- -D warnings`、`npm run spectree:check`。
