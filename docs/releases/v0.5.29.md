# v0.5.29

- TUI 增加统一的快捷键注册表、命令面板、上下文帮助与可配置的键位覆盖；快捷键配置冲突会原子回退到默认值。
- 快照落盘转为有界后台写入，并新增渲染、事件到帧、序列化和 I/O 的性能遥测及 Windows ConPTY 回归门。
- 安装器的首次配置骨架统一为通用 OpenAI 端点；Windows 支持 `-NoPath -NoConfig`，Unix 支持 `--no-config`，方便 CI 与受限环境安装。
- README 补充可复现安装、受限网络/脚本策略下的离线安装路径和第三方 OpenAI-compatible provider 配置。
