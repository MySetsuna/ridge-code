# M3 eval 闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付独立二进制 `rc-eval`,在内置小任务集上跑「全程强模型单 agent」基线 vs「混合编排」两种模式,真实/离线两套运行,用隐藏验收测试客观判定,产出含 USD 的成本-质量对照。

**Architecture:** `rc-eval` 装配 provider(真实 / 离线 Stub)与定价后,对每个任务×每种模式:复制 seed 到临时副本 → 在副本里跑模式(基线走 `rc-core::run_single`,编排走 `Orchestrator::run`)→ 注入隐藏验收测试 → 跑 `cargo test` 判定 → 记一条 `TaskOutcome`;最后汇总成对照表 + JSON。真实/离线只在装配阶段换 `LlmProvider` 实现,`runner`/`rc-core` 无感知。

**Tech Stack:** Rust 2021 workspace,tokio,clap,serde/serde_json,anyhow;复用 `rc-core`(编排)、`rc-providers`(provider trait)、`rc-verify`(跑验收)、`rc-types`(数据)。

## Global Constraints

- Rust edition 2021;依赖版本统一在根 `Cargo.toml` 的 `[workspace.dependencies]`,子 crate 一律 `dep.workspace = true`。
- `rc-types` 保持纯数据 + serde,零业务逻辑。
- 第三方 provider 必须包在 `rc-providers::LlmProvider` trait 后;`rc-core` 只依赖该 trait,不碰具体实现。
- 可观测性走 `tracing`;`println!` 仅用于面向用户的报表输出。
- 离线模式必须**零联网、零成本**(不读 `~/.ridge/config.toml`、不发网络请求)。
- 跨平台:跑子进程命令用 `cfg!(windows)` 分支(沿用 `rc-verify`/`rc-tools` 既有写法)。
- 密钥永不写日志。
- `rc-tools` 的文件工具基于**进程全局 cwd**:故 eval 串行跑,每个任务跑前 `set_current_dir(副本)`,跑完恢复原 cwd。

---

## 任务依赖与并发分组

- **可并行起步(相互独立):** Task 1(rc-types 定价)、Task 2(StubProvider)、Task 4(add-mul fixture)、Task 5(fix-compile fixture)。
- **依赖关系:** Task 3 依赖 2;Task 6 独立;Task 7 依赖 4、5、6;Task 8 依赖 1、6;Task 9 依赖 1、2、3、6、7;Task 10 依赖全部。
- 按既定策略:独立任务(1/2/4/5)可派并发 subagent;有依赖、要对接缝的(3/6/7/8/9/10)主会话串行做。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `crates/rc-types/src/lib.rs` | 加 `Rate`/`Pricing`(纯数据 + 纯算术折算) | Modify |
| `crates/rc-providers/src/stub.rs` | `StubProvider`:脚本化假模型 | Create |
| `crates/rc-providers/src/lib.rs` | 挂载 `stub` 模块、导出 `StubProvider` | Modify |
| `crates/rc-core/src/lib.rs` | 加 `Orchestrator::run_single`(基线) | Modify |
| `crates/rc-eval/tasks/add-mul/**` | fixture:prompt + seed + 隐藏验收 | Create |
| `crates/rc-eval/tasks/fix-compile/**` | fixture | Create |
| `crates/rc-eval/Cargo.toml` | 加依赖 + `[[bin]]` | Modify |
| `crates/rc-eval/src/lib.rs` | 模块声明 + `RunMode`/`TaskOutcome` | Modify |
| `crates/rc-eval/src/tasks.rs` | `EvalTask` + `builtin_tasks()` | Create |
| `crates/rc-eval/src/reporter.rs` | 汇总指标 + 表格 + JSON | Create |
| `crates/rc-eval/src/runner.rs` | 跑一条 task×mode;离线 provider 构造 | Create |
| `crates/rc-eval/src/main.rs` | clap + 装配 + 驱动 | Create |

---

## Task 1: rc-types 加定价类型

**Files:**
- Modify: `crates/rc-types/src/lib.rs`(在文件末尾追加)

**Interfaces:**
- Consumes: 无。
- Produces: `Rate { in_per_mtok: f64, out_per_mtok: f64 }`、`Pricing { strong: Rate, weak: Rate }`、`Rate::cost_usd(&self, in_tok: u64, out_tok: u64) -> f64`(纯算术:`in_tok/1e6*in_per_mtok + out_tok/1e6*out_per_mtok`)。

- [ ] **Step 1: 写失败测试**

在 `crates/rc-types/src/lib.rs` 末尾追加:
```rust
#[cfg(test)]
mod pricing_tests {
    use super::*;

    #[test]
    fn rate_cost_usd_is_per_million() {
        let r = Rate { in_per_mtok: 3.0, out_per_mtok: 15.0 };
        // 100 万 in * $3 + 100 万 out * $15 = $18
        assert!((r.cost_usd(1_000_000, 1_000_000) - 18.0).abs() < 1e-9);
        assert!((r.cost_usd(0, 0)).abs() < 1e-9);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rc-types rate_cost_usd_is_per_million`
Expected: 编译失败 —— `cannot find type Rate`。

- [ ] **Step 3: 写实现**

在 `crates/rc-types/src/lib.rs` 末尾(测试模块之前)追加:
```rust
/// 每百万 token 的美元单价。
#[derive(Debug, Clone, Copy, Default, Serialize, Deserialize)]
pub struct Rate {
    pub in_per_mtok: f64,
    pub out_per_mtok: f64,
}

impl Rate {
    /// 纯算术:给定输入/输出 token 数,折算成美元。
    pub fn cost_usd(&self, in_tok: u64, out_tok: u64) -> f64 {
        (in_tok as f64) / 1_000_000.0 * self.in_per_mtok
            + (out_tok as f64) / 1_000_000.0 * self.out_per_mtok
    }
}

/// 强 / 弱两档定价。
#[derive(Debug, Clone, Copy, Default, Serialize, Deserialize)]
pub struct Pricing {
    pub strong: Rate,
    pub weak: Rate,
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rc-types rate_cost_usd_is_per_million`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-types/src/lib.rs
git commit -m "feat(types): 加 Rate/Pricing 定价类型与 USD 折算"
```

---

## Task 2: rc-providers 加 StubProvider

**Files:**
- Create: `crates/rc-providers/src/stub.rs`
- Modify: `crates/rc-providers/src/lib.rs`(顶部加模块挂载与导出)

**Interfaces:**
- Consumes: `rc_types::{Completion, Message, Role, Usage, ToolSpec}`、本 crate 的 `LlmProvider`。
- Produces: `StubProvider`、`StubProvider::new(model: impl Into<String>, replies: Vec<Completion>) -> Self`。行为:每次 `complete` 弹出队首预制 `Completion`;队列空时返回一个无工具调用的「完成。」文本 `Completion`(usage 10/5)。

- [ ] **Step 1: 写失败测试**

创建 `crates/rc-providers/src/stub.rs`:
```rust
//! 脚本化假模型 provider(离线 eval / 单测用)。按调用顺序返回预制回复,
//! 队列耗尽后返回一个无工具调用的「完成。」文本——足以驱动 agent 工具循环收敛。

use std::collections::VecDeque;
use std::sync::Mutex;

use anyhow::Result;
use async_trait::async_trait;
use rc_types::{Completion, Message, Role, ToolSpec, Usage};

use crate::LlmProvider;

pub struct StubProvider {
    model: String,
    replies: Mutex<VecDeque<Completion>>,
}

impl StubProvider {
    pub fn new(model: impl Into<String>, replies: Vec<Completion>) -> Self {
        Self { model: model.into(), replies: Mutex::new(replies.into()) }
    }
}

#[async_trait]
impl LlmProvider for StubProvider {
    async fn complete(&self, _messages: &[Message], _tools: &[ToolSpec]) -> Result<Completion> {
        let mut q = self.replies.lock().unwrap();
        Ok(q.pop_front().unwrap_or_else(|| Completion {
            message: Message {
                role: Role::Assistant,
                content: "完成。".into(),
                tool_calls: Vec::new(),
                tool_call_id: None,
            },
            usage: Usage { input_tokens: 10, output_tokens: 5 },
        }))
    }

    fn model_id(&self) -> &str {
        &self.model
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn returns_scripted_then_default() {
        let scripted = Completion {
            message: Message {
                role: Role::Assistant,
                content: "脚本回复".into(),
                tool_calls: Vec::new(),
                tool_call_id: None,
            },
            usage: Usage { input_tokens: 1, output_tokens: 1 },
        };
        let p = StubProvider::new("stub", vec![scripted]);
        let r1 = p.complete(&[], &[]).await.unwrap();
        assert_eq!(r1.message.content, "脚本回复");
        let r2 = p.complete(&[], &[]).await.unwrap();
        assert_eq!(r2.message.content, "完成。");
        assert!(r2.message.tool_calls.is_empty());
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rc-providers returns_scripted_then_default`
Expected: 编译失败 —— `stub` 模块未挂载,`StubProvider` 不可见。

- [ ] **Step 3: 挂载模块并导出**

在 `crates/rc-providers/src/lib.rs` 顶部(`use` 之后、`pub trait LlmProvider` 之前)加:
```rust
mod stub;
pub use stub::StubProvider;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rc-providers returns_scripted_then_default`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-providers/src/stub.rs crates/rc-providers/src/lib.rs
git commit -m "feat(providers): 加 StubProvider(脚本化假模型,离线 eval/单测用)"
```

---

## Task 3: rc-core 加单 agent 基线入口 run_single

**Files:**
- Modify: `crates/rc-core/src/lib.rs`(给 `impl Orchestrator` 加方法 + 文件末尾加测试)
- Modify: `crates/rc-core/Cargo.toml`(无需改:`rc-providers` 已是依赖,`tokio` 测试用 —— 见 Step 2 备注)

**Interfaces:**
- Consumes: 现有 `run_agent`、`verify_and_repair`、`WORKER_SYSTEM`、`Outcome`、`Cost`、`ModelTier`、`Message`。
- Produces: `Orchestrator::run_single(&self, task: &str) -> anyhow::Result<Outcome>`。语义:**不含 Planner/Router/Reviewer**,强模型直接跑工具循环 + 验证修复;返回 `Outcome { subtasks: 1, reviewed: false, approved: true, .. }`。

- [ ] **Step 1: 写失败测试**

在 `crates/rc-core/src/lib.rs` 末尾追加:
```rust
#[cfg(test)]
mod run_single_tests {
    use super::*;
    use rc_providers::{LlmProvider, StubProvider};
    use rc_types::{Completion, Message, Role, Usage};

    #[tokio::test]
    async fn run_single_is_single_subtask_no_review() {
        let done = Completion {
            message: Message { role: Role::Assistant, content: "done".into(), tool_calls: vec![], tool_call_id: None },
            usage: Usage { input_tokens: 5, output_tokens: 5 },
        };
        let strong: Box<dyn LlmProvider> = Box::new(StubProvider::new("s", vec![done]));
        let weak: Box<dyn LlmProvider> = Box::new(StubProvider::new("w", vec![]));
        // 空临时目录:无 Cargo.toml/ridge.toml → 验证 Uncertain → 视为通过。
        let tmp = std::env::temp_dir().join("rc-core-run-single-test");
        std::fs::create_dir_all(&tmp).unwrap();

        let orch = Orchestrator::new(strong, weak, tmp, OrchestratorConfig::default());
        let out = orch.run_single("加一个函数").await.unwrap();

        assert_eq!(out.subtasks, 1);
        assert!(!out.reviewed);
        assert!(out.cost.strong_tokens() > 0);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rc-core run_single_is_single_subtask_no_review`
Expected: 编译失败 —— `no method named run_single`。
> 备注:若报 `tokio` 宏不可用,在 `crates/rc-core/Cargo.toml` 加 `[dev-dependencies]\ntokio = { workspace = true }`(workspace 的 tokio 含 `macros`/`rt` feature)。

- [ ] **Step 3: 写实现**

在 `crates/rc-core/src/lib.rs` 的 `impl Orchestrator { ... }` 块内(紧接 `run` 方法之后)加:
```rust
    /// 基线:全程强模型单 agent —— 不分解/不路由/不评审,直接工具循环 + 验证修复。
    pub async fn run_single(&self, task: &str) -> Result<Outcome> {
        let mut cost = Cost::default();
        info!("基线:全程强模型单 agent 直跑");
        let user = format!(
            "请完成以下编码任务,写的代码应能通过编译。完成后用一句中文总结并停止:\n{task}"
        );
        let mut msgs = vec![Message::system(WORKER_SYSTEM), Message::user(user)];
        run_agent(
            self.strong.as_ref(),
            ModelTier::Strong,
            &mut msgs,
            &self.tools,
            self.cfg.max_steps,
            &mut cost,
        )
        .await?;
        let repairs = self.verify_and_repair(&mut cost).await?;
        Ok(Outcome { subtasks: 1, repairs, reviewed: false, approved: true, cost })
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rc-core run_single_is_single_subtask_no_review`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-core/src/lib.rs crates/rc-core/Cargo.toml
git commit -m "feat(core): 加 run_single 基线入口(全强单 agent,不分解/不评审)"
```

---

## Task 4: add-mul fixture

**Files:**
- Create: `crates/rc-eval/tasks/add-mul/prompt.txt`
- Create: `crates/rc-eval/tasks/add-mul/seed/Cargo.toml`
- Create: `crates/rc-eval/tasks/add-mul/seed/src/lib.rs`
- Create: `crates/rc-eval/tasks/add-mul/accept/tests/acceptance.rs`

**Interfaces:**
- Produces: 一个 crate 名 `add_mul`、公共 API `pub fn add(a: i64, b: i64) -> i64` 与 `pub fn mul(a: i64, b: i64) -> i64`;隐藏验收 `tests/acceptance.rs`。Task 7 的 `builtin_tasks()` 依赖这些路径与签名。

- [ ] **Step 1: 创建 prompt.txt**

`crates/rc-eval/tasks/add-mul/prompt.txt`:
```
在当前 Rust 库 crate(crate 名 add_mul)中,于 src/lib.rs 实现两个公共函数,签名必须严格如下:

    pub fn add(a: i64, b: i64) -> i64   // 返回 a + b
    pub fn mul(a: i64, b: i64) -> i64   // 返回 a * b

要求代码能通过 cargo build。完成后用一句中文总结并停止。
```

- [ ] **Step 2: 创建 seed(起始空骨架)**

`crates/rc-eval/tasks/add-mul/seed/Cargo.toml`:
```toml
[package]
name = "add-mul"
version = "0.0.0"
edition = "2021"
```

`crates/rc-eval/tasks/add-mul/seed/src/lib.rs`:
```rust
//! 按 prompt 实现 add 与 mul(签名见 prompt)。
```

- [ ] **Step 3: 创建隐藏验收测试**

`crates/rc-eval/tasks/add-mul/accept/tests/acceptance.rs`:
```rust
use add_mul::{add, mul};

#[test]
fn acceptance_add() {
    assert_eq!(add(2, 3), 5);
    assert_eq!(add(-1, 1), 0);
}

#[test]
fn acceptance_mul() {
    assert_eq!(mul(2, 3), 6);
    assert_eq!(mul(0, 9), 0);
}
```

- [ ] **Step 4: 验证 seed 是独立可编译项目**

Run: `cargo build --manifest-path crates/rc-eval/tasks/add-mul/seed/Cargo.toml`
Expected: 编译通过(空库,无内容也能 build)。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-eval/tasks/add-mul
git commit -m "test(eval): 加 add-mul fixture(seed + prompt + 隐藏验收)"
```

---

## Task 5: fix-compile fixture

**Files:**
- Create: `crates/rc-eval/tasks/fix-compile/prompt.txt`
- Create: `crates/rc-eval/tasks/fix-compile/seed/Cargo.toml`
- Create: `crates/rc-eval/tasks/fix-compile/seed/src/lib.rs`
- Create: `crates/rc-eval/tasks/fix-compile/accept/tests/acceptance.rs`

**Interfaces:**
- Produces: crate 名 `fix_compile`、公共 API `pub fn double(x: i64) -> i64`;seed 初始**编译不过**(类型错误),需 agent 修复。Task 7 依赖这些路径与签名。

- [ ] **Step 1: 创建 prompt.txt**

`crates/rc-eval/tasks/fix-compile/prompt.txt`:
```
当前 Rust 库 crate(crate 名 fix_compile)的 src/lib.rs 编译不过。请修复它,使:

    pub fn double(x: i64) -> i64   // 返回 x * 2

能正确编译并返回 x 的两倍。不要改函数签名。完成后用一句中文总结并停止。
```

- [ ] **Step 2: 创建 seed(故意编译不过)**

`crates/rc-eval/tasks/fix-compile/seed/Cargo.toml`:
```toml
[package]
name = "fix-compile"
version = "0.0.0"
edition = "2021"
```

`crates/rc-eval/tasks/fix-compile/seed/src/lib.rs`:
```rust
pub fn double(x: i64) -> i64 {
    // 类型错误:把 i64 赋给 String,无法编译。
    let y: String = x * 2;
    y
}
```

- [ ] **Step 3: 创建隐藏验收测试**

`crates/rc-eval/tasks/fix-compile/accept/tests/acceptance.rs`:
```rust
use fix_compile::double;

#[test]
fn acceptance_double() {
    assert_eq!(double(21), 42);
    assert_eq!(double(0), 0);
    assert_eq!(double(-3), -6);
}
```

- [ ] **Step 4: 验证 seed 确实编译不过**

Run: `cargo build --manifest-path crates/rc-eval/tasks/fix-compile/seed/Cargo.toml`
Expected: **编译失败**(mismatched types: expected `String`, found `i64`)。这正是该 fixture 要的初始状态。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-eval/tasks/fix-compile
git commit -m "test(eval): 加 fix-compile fixture(编译错误待修 + 隐藏验收)"
```

---

## Task 6: rc-eval crate 骨架(依赖 + bin + 顶层类型)

**Files:**
- Modify: `crates/rc-eval/Cargo.toml`
- Modify: `crates/rc-eval/src/lib.rs`

**Interfaces:**
- Produces: `rc_eval::RunMode { Baseline, Orchestrated }`(`Copy + PartialEq + Serialize` rename lowercase)、`rc_eval::TaskOutcome { task: String, mode: RunMode, success: bool, cost: rc_types::Cost, usd: f64, elapsed_ms: u128, error: Option<String> }`、模块声明 `pub mod tasks; pub mod runner; pub mod reporter;`。Task 7/8/9/10 全部依赖这些类型。

- [ ] **Step 1: 改 Cargo.toml**

把 `crates/rc-eval/Cargo.toml` 整体替换为:
```toml
[package]
name = "rc-eval"
edition.workspace = true
version.workspace = true

[[bin]]
name = "rc-eval"
path = "src/main.rs"

[dependencies]
rc-types = { path = "../rc-types" }
rc-core = { path = "../rc-core" }
rc-providers = { path = "../rc-providers" }
rc-verify = { path = "../rc-verify" }
anyhow = { workspace = true }
clap = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
dotenvy = { workspace = true }
```

- [ ] **Step 2: 写顶层类型 + 模块声明(含占位模块,保证可编译)**

把 `crates/rc-eval/src/lib.rs` 整体替换为:
```rust
//! rc-eval — eval harness(M3 最小闭环)。详见 docs/superpowers/specs/2026-06-25-m3-eval-design.md。

pub mod reporter;
pub mod runner;
pub mod tasks;

use rc_types::Cost;
use serde::Serialize;

/// 一次运行采用的模式。
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize)]
#[serde(rename_all = "lowercase")]
pub enum RunMode {
    /// 全程强模型单 agent(基线)。
    Baseline,
    /// 强/弱混合编排。
    Orchestrated,
}

/// 「一个任务 × 一种模式」的结果。
#[derive(Debug, Clone, Serialize)]
pub struct TaskOutcome {
    pub task: String,
    pub mode: RunMode,
    pub success: bool,
    pub cost: Cost,
    pub usd: f64,
    pub elapsed_ms: u128,
    pub error: Option<String>,
}
```

- [ ] **Step 3: 写最小占位模块让 crate 编译**

创建 `crates/rc-eval/src/tasks.rs`:
```rust
//! 占位,Task 7 实现。
```
创建 `crates/rc-eval/src/reporter.rs`:
```rust
//! 占位,Task 8 实现。
```
创建 `crates/rc-eval/src/runner.rs`:
```rust
//! 占位,Task 9 实现。
```
创建 `crates/rc-eval/src/main.rs`:
```rust
fn main() {
    println!("rc-eval 占位,Task 10 实现");
}
```

- [ ] **Step 4: 编译确认通过**

Run: `cargo build -p rc-eval`
Expected: 编译通过。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-eval/Cargo.toml crates/rc-eval/src
git commit -m "feat(eval): rc-eval 骨架(依赖/bin/RunMode/TaskOutcome + 占位模块)"
```

---

## Task 7: tasks 模块(EvalTask + 内置任务集)

**Files:**
- Modify: `crates/rc-eval/src/tasks.rs`(替换占位)

**Interfaces:**
- Consumes: Task 4/5 的 fixture 文件(`include_str!` 编译期内联 prompt;`CARGO_MANIFEST_DIR` 运行期定位 seed/accept)。
- Produces: `EvalTask { name: String, prompt: String, seed_dir: PathBuf, accept_dir: PathBuf, solution_files: Vec<(String, String)>, plan_json: String }`、`builtin_tasks() -> Vec<EvalTask>`。`solution_files` 是离线 stub 写入的金标准解答;`plan_json` 是离线编排模式 planner 的预制计划(单个 trivial 子任务 → 路由到弱)。

- [ ] **Step 1: 写失败测试**

在 `crates/rc-eval/src/tasks.rs` 写(连同实现一起,先写测试块在文件末尾):
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn builtin_tasks_present_and_well_formed() {
        let tasks = builtin_tasks();
        assert_eq!(tasks.len(), 2);
        for t in &tasks {
            assert!(!t.prompt.trim().is_empty(), "{} prompt 为空", t.name);
            assert!(t.seed_dir.join("Cargo.toml").exists(), "{} 缺 seed Cargo.toml", t.name);
            assert!(t.accept_dir.join("tests/acceptance.rs").exists(), "{} 缺验收", t.name);
            assert!(!t.solution_files.is_empty(), "{} 缺 solution", t.name);
            // plan_json 必须能解析成非空数组
            let v: serde_json::Value = serde_json::from_str(&t.plan_json).unwrap();
            assert!(v.as_array().map(|a| !a.is_empty()).unwrap_or(false), "{} plan_json 非法", t.name);
        }
    }
}
```
> 备注:测试用到 `serde_json`,已在依赖中。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rc-eval builtin_tasks_present_and_well_formed`
Expected: 编译失败 —— `EvalTask`/`builtin_tasks` 未定义。

- [ ] **Step 3: 写实现(在 tasks.rs 顶部,测试块之上)**

```rust
//! 内置 eval 任务集:每个任务自带 prompt、起始 seed、隐藏验收,
//! 以及离线模式所需的金标准解答(solution_files)与编排计划(plan_json)。

use std::path::PathBuf;

pub struct EvalTask {
    pub name: String,
    pub prompt: String,
    pub seed_dir: PathBuf,
    pub accept_dir: PathBuf,
    /// 离线 stub 写入的正确解答:(相对路径, 文件内容)。
    pub solution_files: Vec<(String, String)>,
    /// 离线编排模式 planner 预制回复(单个 trivial 子任务 → 路由到弱模型执行)。
    pub plan_json: String,
}

fn tasks_root() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tasks")
}

pub fn builtin_tasks() -> Vec<EvalTask> {
    let root = tasks_root();
    vec![
        EvalTask {
            name: "add-mul".into(),
            prompt: include_str!("../tasks/add-mul/prompt.txt").into(),
            seed_dir: root.join("add-mul/seed"),
            accept_dir: root.join("add-mul/accept"),
            solution_files: vec![(
                "src/lib.rs".into(),
                "pub fn add(a: i64, b: i64) -> i64 { a + b }\npub fn mul(a: i64, b: i64) -> i64 { a * b }\n".into(),
            )],
            plan_json: r#"[{"id":"s1","description":"实现 add 和 mul 函数","deps":[],"difficulty":"trivial"}]"#.into(),
        },
        EvalTask {
            name: "fix-compile".into(),
            prompt: include_str!("../tasks/fix-compile/prompt.txt").into(),
            seed_dir: root.join("fix-compile/seed"),
            accept_dir: root.join("fix-compile/accept"),
            solution_files: vec![(
                "src/lib.rs".into(),
                "pub fn double(x: i64) -> i64 { x * 2 }\n".into(),
            )],
            plan_json: r#"[{"id":"s1","description":"修复 double 使其编译且正确","deps":[],"difficulty":"trivial"}]"#.into(),
        },
    ]
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rc-eval builtin_tasks_present_and_well_formed`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-eval/src/tasks.rs
git commit -m "feat(eval): EvalTask + builtin_tasks(add-mul / fix-compile)"
```

---

## Task 8: reporter 模块(汇总指标 + 表格 + JSON)

**Files:**
- Modify: `crates/rc-eval/src/reporter.rs`(替换占位)

**Interfaces:**
- Consumes: `crate::{RunMode, TaskOutcome}`、`rc_types::Cost`。
- Produces: `ModeSummary { mode: RunMode, total: usize, passed: usize, total_usd: f64, strong_share: f64, total_ms: u128 }`、`summarize(&[TaskOutcome]) -> Vec<ModeSummary>`、`render(&[ModeSummary]) -> String`、`write_json(&[TaskOutcome], &Path) -> anyhow::Result<()>`。

- [ ] **Step 1: 写失败测试**

在 `crates/rc-eval/src/reporter.rs` 末尾写:
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::{RunMode, TaskOutcome};
    use rc_types::Cost;

    fn outcome(mode: RunMode, success: bool, c: Cost, usd: f64) -> TaskOutcome {
        TaskOutcome { task: "t".into(), mode, success, cost: c, usd, elapsed_ms: 10, error: None }
    }

    #[test]
    fn summarize_computes_rates_and_share() {
        let outcomes = vec![
            outcome(RunMode::Baseline, true, Cost { strong_in: 100, strong_out: 50, weak_in: 0, weak_out: 0 }, 0.5),
            outcome(RunMode::Orchestrated, true, Cost { strong_in: 20, strong_out: 10, weak_in: 80, weak_out: 40 }, 0.1),
        ];
        let s = summarize(&outcomes);
        assert_eq!(s.len(), 2);
        let base = s.iter().find(|m| m.mode == RunMode::Baseline).unwrap();
        assert_eq!(base.passed, 1);
        assert!((base.total_usd - 0.5).abs() < 1e-9);
        assert!((base.strong_share - 1.0).abs() < 1e-9);
        let orch = s.iter().find(|m| m.mode == RunMode::Orchestrated).unwrap();
        assert!((orch.strong_share - 0.2).abs() < 1e-9); // 30 / 150
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rc-eval summarize_computes_rates_and_share`
Expected: 编译失败 —— `summarize`/`ModeSummary` 未定义。

- [ ] **Step 3: 写实现(reporter.rs 顶部,测试块之上)**

```rust
//! 汇总 TaskOutcome → 指标与对照表 + JSON 存档。

use std::path::Path;

use anyhow::{Context, Result};
use rc_types::Cost;

use crate::{RunMode, TaskOutcome};

#[derive(Debug, Clone)]
pub struct ModeSummary {
    pub mode: RunMode,
    pub total: usize,
    pub passed: usize,
    pub total_usd: f64,
    pub strong_share: f64,
    pub total_ms: u128,
}

pub fn summarize(outcomes: &[TaskOutcome]) -> Vec<ModeSummary> {
    let mut summaries = Vec::new();
    for mode in [RunMode::Baseline, RunMode::Orchestrated] {
        let items: Vec<&TaskOutcome> = outcomes.iter().filter(|o| o.mode == mode).collect();
        if items.is_empty() {
            continue;
        }
        let mut agg = Cost::default();
        let mut total_usd = 0.0;
        let mut passed = 0usize;
        let mut total_ms = 0u128;
        for o in &items {
            agg.strong_in += o.cost.strong_in;
            agg.strong_out += o.cost.strong_out;
            agg.weak_in += o.cost.weak_in;
            agg.weak_out += o.cost.weak_out;
            total_usd += o.usd;
            total_ms += o.elapsed_ms;
            if o.success {
                passed += 1;
            }
        }
        summaries.push(ModeSummary {
            mode,
            total: items.len(),
            passed,
            total_usd,
            strong_share: agg.strong_share(),
            total_ms,
        });
    }
    summaries
}

pub fn render(summaries: &[ModeSummary]) -> String {
    let mut out = String::from("\n──────── ridge-code eval 报告 ────────\n");
    out.push_str("模式          通过/总   总成本(USD)   强模型占比   总耗时(ms)\n");
    for s in summaries {
        out.push_str(&format!(
            "{:<12}  {}/{}      ${:<10.4}  {:>6.0}%      {}\n",
            format!("{:?}", s.mode),
            s.passed,
            s.total,
            s.total_usd,
            s.strong_share * 100.0,
            s.total_ms,
        ));
    }
    let base = summaries.iter().find(|m| m.mode == RunMode::Baseline);
    let orch = summaries.iter().find(|m| m.mode == RunMode::Orchestrated);
    if let (Some(b), Some(o)) = (base, orch) {
        let saving = if b.total_usd > 0.0 {
            (b.total_usd - o.total_usd) / b.total_usd * 100.0
        } else {
            0.0
        };
        out.push_str(&format!(
            "\n对比:编排相对基线节省成本 {:.0}%;质量 通过 {}/{} vs {}/{}(越接近越好)\n",
            saving, o.passed, o.total, b.passed, b.total
        ));
    }
    out
}

pub fn write_json(outcomes: &[TaskOutcome], path: &Path) -> Result<()> {
    if let Some(parent) = path.parent() {
        std::fs::create_dir_all(parent).ok();
    }
    let json = serde_json::to_string_pretty(outcomes).context("序列化结果失败")?;
    std::fs::write(path, json).with_context(|| format!("写 {} 失败", path.display()))?;
    Ok(())
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rc-eval summarize_computes_rates_and_share`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-eval/src/reporter.rs
git commit -m "feat(eval): reporter(汇总成功率/USD/强占比 + 对照表 + JSON)"
```

---

## Task 9: runner 模块(跑一条 task×mode)

**Files:**
- Modify: `crates/rc-eval/src/runner.rs`(替换占位)

**Interfaces:**
- Consumes: `crate::{RunMode, TaskOutcome}`、`crate::tasks::EvalTask`、`rc_core::{Orchestrator, OrchestratorConfig}`、`rc_providers::{LlmProvider, StubProvider}`、`rc_verify::{verify, VerifyPlan, Check}`、`rc_types::{Completion, Message, Role, ToolCall, Usage, Pricing, Cost}`。
- Produces: `run_one(task: &EvalTask, mode: RunMode, strong: Box<dyn LlmProvider>, weak: Box<dyn LlmProvider>, pricing: &Pricing, keep: bool) -> TaskOutcome`(不返回 Result:错误进 `TaskOutcome.error`)、`offline_providers(task: &EvalTask, mode: RunMode) -> (Box<dyn LlmProvider>, Box<dyn LlmProvider>)`、私有 `copy_dir_all`。

- [ ] **Step 1: 写失败测试(目录复制 helper)**

在 `crates/rc-eval/src/runner.rs` 末尾写:
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn copy_dir_all_copies_nested() {
        let base = std::env::temp_dir().join("rc-eval-copy-test");
        let _ = std::fs::remove_dir_all(&base);
        let src = base.join("src");
        std::fs::create_dir_all(src.join("a")).unwrap();
        std::fs::write(src.join("a/f.txt"), "hi").unwrap();
        let dst = base.join("dst");
        copy_dir_all(&src, &dst).unwrap();
        assert_eq!(std::fs::read_to_string(dst.join("a/f.txt")).unwrap(), "hi");
        let _ = std::fs::remove_dir_all(&base);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rc-eval copy_dir_all_copies_nested`
Expected: 编译失败 —— `copy_dir_all` 未定义。

- [ ] **Step 3: 写实现(runner.rs 顶部,测试块之上)**

```rust
//! 跑「一个任务 × 一种模式」:复制 seed → 跑模式 → 注入隐藏验收 → 判定 → TaskOutcome。

use std::path::Path;
use std::time::Instant;

use anyhow::Result;
use rc_core::{Orchestrator, OrchestratorConfig};
use rc_providers::{LlmProvider, StubProvider};
use rc_types::{Completion, Cost, Message, Pricing, Role, ToolCall, Usage};
use rc_verify::{verify, Check, VerifyPlan};

use crate::tasks::EvalTask;
use crate::{RunMode, TaskOutcome};

/// 离线模式:按任务/模式构造脚本化假模型。
/// - 基线:强 = 一次性写入解答;弱不参与。
/// - 编排:强 = 返回计划(单 trivial 子任务);弱 = 写入解答。
pub fn offline_providers(
    task: &EvalTask,
    mode: RunMode,
) -> (Box<dyn LlmProvider>, Box<dyn LlmProvider>) {
    match mode {
        RunMode::Baseline => (
            Box::new(StubProvider::new("stub-strong", vec![write_file_completion(&task.solution_files)])),
            Box::new(StubProvider::new("stub-weak", vec![])),
        ),
        RunMode::Orchestrated => (
            Box::new(StubProvider::new("stub-strong", vec![plan_completion(&task.plan_json)])),
            Box::new(StubProvider::new("stub-weak", vec![write_file_completion(&task.solution_files)])),
        ),
    }
}

fn write_file_completion(files: &[(String, String)]) -> Completion {
    let tool_calls = files
        .iter()
        .enumerate()
        .map(|(i, (path, content))| ToolCall {
            id: format!("call_{i}"),
            name: "write_file".into(),
            arguments: serde_json::json!({ "path": path, "content": content }).to_string(),
        })
        .collect();
    Completion {
        message: Message { role: Role::Assistant, content: String::new(), tool_calls, tool_call_id: None },
        usage: Usage { input_tokens: 200, output_tokens: 80 },
    }
}

fn plan_completion(plan_json: &str) -> Completion {
    Completion {
        message: Message {
            role: Role::Assistant,
            content: plan_json.to_string(),
            tool_calls: Vec::new(),
            tool_call_id: None,
        },
        usage: Usage { input_tokens: 150, output_tokens: 40 },
    }
}

fn copy_dir_all(src: &Path, dst: &Path) -> std::io::Result<()> {
    std::fs::create_dir_all(dst)?;
    for entry in std::fs::read_dir(src)? {
        let entry = entry?;
        let ty = entry.file_type()?;
        let to = dst.join(entry.file_name());
        if ty.is_dir() {
            copy_dir_all(&entry.path(), &to)?;
        } else {
            std::fs::copy(entry.path(), &to)?;
        }
    }
    Ok(())
}

pub async fn run_one(
    task: &EvalTask,
    mode: RunMode,
    strong: Box<dyn LlmProvider>,
    weak: Box<dyn LlmProvider>,
    pricing: &Pricing,
    keep: bool,
) -> TaskOutcome {
    let started = Instant::now();
    let orig = std::env::current_dir().expect("读取当前目录失败");
    let work = std::env::temp_dir().join(format!("rc-eval-{}-{:?}", task.name, mode));

    let res = run_inner(task, mode, strong, weak, &work).await;

    // 务必恢复全局 cwd(rc-tools 用进程 cwd;后续报表写相对路径)。
    let _ = std::env::set_current_dir(&orig);
    if keep {
        tracing::info!(path = %work.display(), "保留工作副本");
    } else {
        let _ = std::fs::remove_dir_all(&work);
    }

    let elapsed_ms = started.elapsed().as_millis();
    match res {
        Ok((success, cost)) => {
            let usd = pricing.strong.cost_usd(cost.strong_in, cost.strong_out)
                + pricing.weak.cost_usd(cost.weak_in, cost.weak_out);
            TaskOutcome { task: task.name.clone(), mode, success, cost, usd, elapsed_ms, error: None }
        }
        Err(e) => TaskOutcome {
            task: task.name.clone(),
            mode,
            success: false,
            cost: Cost::default(),
            usd: 0.0,
            elapsed_ms,
            error: Some(format!("{e:#}")),
        },
    }
}

async fn run_inner(
    task: &EvalTask,
    mode: RunMode,
    strong: Box<dyn LlmProvider>,
    weak: Box<dyn LlmProvider>,
    work: &Path,
) -> Result<(bool, Cost)> {
    let _ = std::fs::remove_dir_all(work);
    copy_dir_all(&task.seed_dir, work)?;
    std::env::set_current_dir(work)?; // 让 rc-tools 文件工具落在副本里

    let orch = Orchestrator::new(strong, weak, work.to_path_buf(), OrchestratorConfig::default());
    let outcome = match mode {
        RunMode::Baseline => orch.run_single(&task.prompt).await?,
        RunMode::Orchestrated => orch.run(&task.prompt).await?,
    };

    // 注入隐藏验收测试,跑 cargo test 客观判定。
    copy_dir_all(&task.accept_dir, work)?;
    let plan = VerifyPlan {
        checks: vec![Check { label: "accept".into(), command: "cargo test".into() }],
    };
    let verdict = verify(&plan, work).await?;
    Ok((verdict.is_pass(), outcome.cost))
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rc-eval copy_dir_all_copies_nested`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-eval/src/runner.rs
git commit -m "feat(eval): runner(复制 seed → 跑模式 → 注入验收 → 判定)"
```

---

## Task 10: main 装配 + 离线集成测试

**Files:**
- Modify: `crates/rc-eval/src/main.rs`(替换占位)
- Create: `crates/rc-eval/tests/offline.rs`(集成测试)

**Interfaces:**
- Consumes: `rc_eval::{reporter, runner, tasks, RunMode}`、`rc_providers::OpenAiCompatProvider`、`rc_types::{Pricing, Rate}`。
- Produces: `rc-eval` 可执行;`--offline` 零成本跑通,`--keep` 保留副本。

- [ ] **Step 1: 写离线集成测试(失败)**

创建 `crates/rc-eval/tests/offline.rs`:
```rust
//! 离线集成:用 StubProvider 跑通内置任务的两种模式,零联网零成本。
//! 慢测(会在临时副本里跑 cargo build/test 子进程),但不烧 key。

use rc_eval::{runner, tasks, RunMode};
use rc_types::{Pricing, Rate};

#[tokio::test]
async fn offline_add_mul_both_modes_pass() {
    let pricing = Pricing {
        strong: Rate { in_per_mtok: 3.0, out_per_mtok: 15.0 },
        weak: Rate { in_per_mtok: 0.5, out_per_mtok: 1.5 },
    };
    let task = tasks::builtin_tasks().into_iter().find(|t| t.name == "add-mul").unwrap();

    for mode in [RunMode::Baseline, RunMode::Orchestrated] {
        let (strong, weak) = runner::offline_providers(&task, mode);
        let out = runner::run_one(&task, mode, strong, weak, &pricing, false).await;
        assert!(out.success, "{:?} 未通过验收: {:?}", mode, out.error);
        assert!(out.usd > 0.0, "{:?} 成本应为正", mode);
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rc-eval --test offline offline_add_mul_both_modes_pass`
Expected: 失败 —— 串行约束未生效前可能因 cwd 竞争或断言失败;首次运行至少应能编译并执行到断言。
> 注:本测试串行执行两种模式,依赖 Task 9 的 `set_current_dir` + 恢复逻辑,故应在 Task 9 完成后才会通过。

- [ ] **Step 3: 写 main 实现**

把 `crates/rc-eval/src/main.rs` 整体替换为:
```rust
//! ridge-code eval harness(M3 最小闭环)。详见 docs/superpowers/specs/2026-06-25-m3-eval-design.md。

use anyhow::{anyhow, Context, Result};
use clap::Parser;
use rc_eval::{reporter, runner, tasks, RunMode};
use rc_providers::{LlmProvider, OpenAiCompatProvider};
use rc_types::{Pricing, Rate};
use serde::Deserialize;
use std::path::PathBuf;

#[derive(Parser)]
#[command(name = "rc-eval", about = "ridge-code eval harness(M3 最小闭环)")]
struct Cli {
    /// 用离线假模型跑(零成本、零联网)
    #[arg(long)]
    offline: bool,
    /// 保留临时工作副本以便调试
    #[arg(long)]
    keep: bool,
}

#[derive(Deserialize, Clone)]
struct ProviderCfg {
    base_url: String,
    model: String,
    #[serde(default)]
    api_key: Option<String>,
    #[serde(default)]
    api_key_env: Option<String>,
    #[serde(default)]
    price_in: Option<f64>,
    #[serde(default)]
    price_out: Option<f64>,
}

#[derive(Deserialize)]
struct FileCfg {
    #[serde(default)]
    strong: Option<ProviderCfg>,
    #[serde(default)]
    weak: Option<ProviderCfg>,
    #[serde(default)]
    provider: Option<ProviderCfg>,
}

fn home_dir() -> Option<PathBuf> {
    std::env::var_os("HOME").or_else(|| std::env::var_os("USERPROFILE")).map(PathBuf::from)
}

fn resolve_api_key(p: &ProviderCfg) -> Result<String> {
    if let Some(k) = &p.api_key {
        if !k.is_empty() {
            return Ok(k.clone());
        }
    }
    let env_name = p.api_key_env.as_deref().unwrap_or("RIDGE_API_KEY");
    std::env::var(env_name).with_context(|| format!("config 未填 api_key,且环境变量 {env_name} 未设置"))
}

fn build_provider(p: &ProviderCfg) -> Result<Box<dyn LlmProvider>> {
    let key = resolve_api_key(p)?;
    Ok(Box::new(OpenAiCompatProvider::new(p.base_url.clone(), key, p.model.clone())))
}

fn rate_of(p: &ProviderCfg) -> Rate {
    Rate { in_per_mtok: p.price_in.unwrap_or(0.0), out_per_mtok: p.price_out.unwrap_or(0.0) }
}

/// 读 ~/.ridge/config.toml,返回 (strong, weak, pricing)。
fn load_real() -> Result<(ProviderCfg, ProviderCfg, Pricing)> {
    let home = home_dir().ok_or_else(|| anyhow!("找不到 HOME / USERPROFILE"))?;
    let path = home.join(".ridge").join("config.toml");
    let text = std::fs::read_to_string(&path)
        .with_context(|| format!("读取配置 {} 失败(可参考仓库 config.example.toml)", path.display()))?;
    let cfg: FileCfg = toml::from_str(&text).context("解析 config.toml 失败")?;
    let strong = cfg.strong.clone().or_else(|| cfg.provider.clone()).context("配置缺少 [strong] 或 [provider]")?;
    let weak = cfg.weak.clone().or_else(|| cfg.provider.clone()).unwrap_or_else(|| strong.clone());
    let pricing = Pricing { strong: rate_of(&strong), weak: rate_of(&weak) };
    Ok((strong, weak, pricing))
}

fn default_pricing() -> Pricing {
    // 离线示意定价(仅用于演示报表;离线 token 来自 stub)。
    Pricing {
        strong: Rate { in_per_mtok: 3.0, out_per_mtok: 15.0 },
        weak: Rate { in_per_mtok: 0.5, out_per_mtok: 1.5 },
    }
}

#[tokio::main]
async fn main() -> Result<()> {
    let _ = dotenvy::from_filename(".env.local");
    let _ = dotenvy::dotenv();
    tracing_subscriber::fmt()
        .with_env_filter(
            tracing_subscriber::EnvFilter::try_from_default_env()
                .unwrap_or_else(|_| tracing_subscriber::EnvFilter::new("info")),
        )
        .init();

    let cli = Cli::parse();
    let tasks = tasks::builtin_tasks();

    let (pricing, real) = if cli.offline {
        (default_pricing(), None)
    } else {
        let (s, w, p) = load_real()?;
        (p, Some((s, w)))
    };

    let mut outcomes = Vec::new();
    for task in &tasks {
        for mode in [RunMode::Baseline, RunMode::Orchestrated] {
            let (strong, weak) = if cli.offline {
                runner::offline_providers(task, mode)
            } else {
                let (s, w) = real.as_ref().unwrap();
                (build_provider(s)?, build_provider(w)?)
            };
            let outcome = runner::run_one(task, mode, strong, weak, &pricing, cli.keep).await;
            println!(
                "· {} [{:?}] success={} usd=${:.4} ({} ms)",
                outcome.task, outcome.mode, outcome.success, outcome.usd, outcome.elapsed_ms
            );
            outcomes.push(outcome);
        }
    }

    let summaries = reporter::summarize(&outcomes);
    println!("{}", reporter::render(&summaries));

    let out_path = PathBuf::from("target/eval/result.json");
    reporter::write_json(&outcomes, &out_path)?;
    println!("结果已写入 {}", out_path.display());
    Ok(())
}
```

- [ ] **Step 4: 跑离线集成测试 + 端到端确认通过**

Run: `cargo test -p rc-eval --test offline offline_add_mul_both_modes_pass`
Expected: PASS(两种模式均通过验收、成本为正)。

Run: `cargo run -p rc-eval -- --offline`
Expected: 控制台逐条打印 4 条结果(2 任务 × 2 模式)success=true,末尾对照表 + `结果已写入 target/eval/result.json`,**全程零联网**。

- [ ] **Step 5: 提交**

```bash
git add crates/rc-eval/src/main.rs crates/rc-eval/tests/offline.rs
git commit -m "feat(eval): main 装配(--offline/--keep)+ 离线集成测试,M3 最小闭环跑通"
```

---

## Self-Review(写计划后自查)

**1. Spec 覆盖:**
- 独立 bin `rc-eval` → Task 6/10。✅
- 内置 2-3 个自包含任务 → Task 4/5(add-mul、fix-compile;第三个为 spec 可选项,不在最小闭环必做)。✅
- 基线 vs 编排 → Task 3(run_single)+ Task 9/10(两模式驱动)。✅
- 真实 + 离线 → Task 2(StubProvider)+ Task 9(offline_providers)+ Task 10(真实 load_real)。✅
- 隐藏验收注入 → Task 9 `run_inner`(跑完模式后复制 accept/ 再 cargo test)。✅
- USD 定价解耦 → Task 1(Rate/Pricing)+ Task 9(折算)+ Task 10(config 读价/默认价)。✅
- 指标/对照表/JSON → Task 8。✅
- 错误不中断、副本清理(--keep) → Task 9 `run_one`(Err 进 outcome、cwd 恢复、remove/keep)。✅
- 测试:StubProvider/runner/reporter 单测 + 离线集成 → Task 2/8/9/10。✅

**2. 占位符扫描:** 无 TBD/TODO 式占位;Task 6 的"占位模块"是被后续任务替换的真实可编译代码,非计划占位。✅

**3. 类型一致性:** `RunMode`/`TaskOutcome` 字段在 Task 6 定义,Task 8/9/10 一致引用;`run_one`/`offline_providers`/`summarize`/`write_json`/`run_single`/`StubProvider::new`/`Rate::cost_usd` 签名跨任务一致。✅
