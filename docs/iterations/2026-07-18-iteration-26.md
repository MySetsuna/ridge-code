# Iteration 26 报告(UI 三部曲 I:主屏内联 REPL + 静态提交)

## 做了什么(只碰 tui.rs + docs,合 contract)

- **范式转向**:备用屏 → `Viewport::Inline(LIVE_HEIGHT=14)` 主屏内联视口。历史内容经 `flush_commits` → `Terminal::insert_before` **静态提交**进终端原生 scrollback(用 ratatui 原生机制,驳回了 NLM 手搓逃逸序列方案);行入历史即永不重绘,Live 视口恒小于终端 —— 闪烁根因根除。
- **Live 视口**:状态行(品牌+provider/model/tokens+**todo d/n 计数**+cwd+spinner)/ 流式尾巴(`stream_tail` 尾 K 行)/ 动态高度输入框;审批模态覆整个视口,**↑↓ 滚动真正接到模态 Paragraph 偏移**(旧版 scroll 误接输出面板,顺手修正)。
- **TODO 呈现**:侧边栏面板删除;变更时 `render_todo_block` 快照静态提交进历史,状态行留进度计数。
- 折行计数抽 `wrapped_rows`,`input_height`/`commit_height` 共用。

## 删了什么(减法同权)

- `EnterAlternateScreen`/`LeaveAlternateScreen`(原生滚动/选取/搜索归还终端)。
- `Ui.log` 环形缓冲 + `LOG_CAP` + `tail_window` + `output_lines`(scrollback 取代自绘滚动)。
- 输出/TODO/工具三面板、`centered_rect`、`List/ListItem` 用法。
- `InputAction::Scroll`(主输入 Up/Down 空置,历史召回留 iter-27)。
- 净减约 90 行,渲染路径大幅简化。

## 测试状态(确定性验收,全达成)

- `cargo test --workspace` **exit 0**(0 败);tui 测试更新+新增:`wrapped_rows` / `commit_height` / `stream_tail` / `commit_queue_drains_in_order` / `todo_progress_and_block_render` / `input_action` 新键位表;审批模态滚动回归保留。删除符号 grep=0(`AlternateScreen`/`tail_window`/`LOG_CAP`)。
- fmt 净;clippy `-D warnings` 净。

## 熔断/停机记录

无触发。

## 开放问题(交 iter-27/28)

- iter-27:CSI u(Shift+Enter 多行)+ 输入历史召回(Up/Down 已空出)+ `/` `@` 补全浮窗。
- iter-28:markdown 渲染、工具输出折叠、启动动效、流式青色呼吸游标、语义化 ANSI 色盘。
- Live 高度 14 固定;超小终端(<14 行)行为待实机验证。
