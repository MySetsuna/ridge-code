# Iteration 25 报告(分支工作区隔离:BoN 真实接入的物理前提)

## 做了什么(合 contract:新模块 + 一行 mod 声明,引擎零改动)

- **新模块 `crates/agent/src/workspace.rs`**:
  - `Workspace` enum(GitWorktree / ShadowCopy)+ `dir()`。
  - `create_isolated(main, dest)`:git 仓库先试 `git worktree add --detach`(best-effort,败即静默回落);否则影子拷贝(跳过 `.git`/`target`/`.ridge`/`node_modules`)。
  - `merge_winner(main, branch_dir, modified_files)`:胜者整文搬运合回(自动建嵌套目录),**全有或全无** —— 读期先取全部真相(零写),写期任一失败回滚已写(原文件复原、新文件删除)。
  - `cleanup`:worktree remove --force(败则 remove_dir_all + prune 兜底)/ 影子直接删;best-effort。
  - `is_git_repo` 纯文件系统探测。

## 测试状态(确定性验收,全达成)

- `cargo test --workspace` **exit 0**(135 过 0 败);新增 5 测:隔离互不踩+胜者合回、嵌套目录合回、缺失文件原子拒绝、`.git` 探测、影子跳过表。全走影子路径,git 不作测试前提(零环境断言)。
- fmt 净;clippy `-D warnings` 净。

## 对抗评审战果(本轮拦下的 NLM 错置)

- `RunConfig.cwd` 注入引擎 → 驳回(app 概念污染引擎,同 BudgetExceeded 先例);隔离归 agent 层。
- `apply_edits` 合回 → 驳回(编辑期原子性≠同步期整文搬运);借其回滚**模式**自写。
- 进程异常退出必清理 → 驳回(进程级信号断言);测函数本身。

## 熔断/停机记录

无触发。

## 开放问题(下一刀首选)

- **BoN 端到端接线**:把分支 `Workspace.dir()` 贯穿 `execute_tool_call` 的路径解析(工具相对路径 rebase 到分支根),`run_once` 加 `--branches N` 入口:N 分支 `invoke_best_of` → `branch_score` 择优 → `merge_winner` → `cleanup`。素材已全,只差接线。
- worktree 真路径未有自动化测试覆盖(避免 git 环境前提);实机使用验证。
