# Iteration 42 报告 —— 减法:砍掉未接线的 BoN/工作区隔离投机基建

> maker = 用户裁定「砍掉 agent 侧孤儿」;checker=我(正确性:代码核实零引用)。价值门禁:纯减法(删被证伪的过度设计,与加法同权)。

## 做了什么(净删 ~200 行)
- **删** `crates/agent/src/workspace.rs` 整文件(`Workspace{GitWorktree|ShadowCopy}` / `create_isolated` / `merge_winner` / `cleanup` + 5 个自测)。
- **删** `lib.rs::branch_score`(BoN 确定性评分器)+ 其自测 `branch_score_prefers_approved_then_cheap`。
- **删** `pub mod workspace;` 声明与其悬空文档注释。
- **保留** `langgraph::invoke_best_of`(通用引擎原语,自带 `best_of_discards_failed_branches` 测)——库层通用能力,非 agent 特定死码。

## 为什么(代码核实,非记忆)
- `workspace.rs` + `branch_score` 自 iter-24/25 造出,**17 轮零生产调用者**(`.rs` 全仓 grep:仅模块自身 + 自测)。`invoke_best_of` agent 从不调用。ARCHITECTURE §2.7 自述「未接 CLI」= 被证伪的提前设计。
- NLM 两版计划自相矛盾(造隔离 / 删隔离+硬接 reviewer),均未接地气;经代码核实后用户拍板砍。
- **驳回 NLM「接 reviewer」**:reviewer 只读回文本,`branch_score` 按 approved+省 token 择优对「评审质量」是错误指标,近乎无用。

## 测试状态(确定性验收,全达成)
`cargo test --workspace && cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings` 全 **exit 0**。
- agent lib **83**(−6:workspace 5 自测 + branch_score 1)/ ridgecode 42 / langgraph **9**(`best_of` 原语测保留)/ 其余 crate 不变。
- 被删符号全仓 `.rs` **零引用**(编译通过即证无残留调用者)。
- ARCHITECTURE §2.7 工作区隔离节删除;§1 `invoke_best_of` 描述改「库原语,agent 侧胶水已删」。

## 对抗评审战果(正确性门禁)
- **以代码为准纠正记忆错误**:上一轮我误判 `FileCheckpointer`/`workspace.rs` 不存在(陈旧记忆),经 grep/codegraph 核实后纠正——**判断以代码为主、文档为辅**。本机陈旧自动记忆已清(`autoMemoryEnabled:false`)。
- **删得干净有据**:先全仓 `.rs` grep 证零外部引用,再删;编译通过 = 编译器背书无残留调用者。
- **保留库原语**:`invoke_best_of` 是 langgraph 通用能力(有测),非 agent 死码,不误删——减法只砍**被证伪的 agent 侧胶水**,不砍通用引擎 API。

## 开放问题 / 下一步
- BoN 若日后真要(为**主 agent**而非只读 reviewer),需重建每分支工作区隔离——git 历史可取回本轮删的 `workspace.rs` 作起点。
- 沙箱(CLAUDE.md「安全层沙箱待做」)、tracing 可观测接入 —— 北极星欠账,后续。

## 熔断/停机记录
无触发。验收:测试一次过;fmt 首轮报删测留下的多余空行,规范化即净;clippy 一次过。
