# Iteration 48 报告 —— pi-agent 式多订阅接入(Codex OAuth + 订阅档一等公民化)

> contract 见 CONTRACT-iteration-48.md;NLM 规划 + 对抗评审见 2026-07-23-notebooklm-guidance-48.md。
> 六目标全落,四提交:af88070(遗留清理,含本轮前置)/ 1011c86(Models 分栏前缀)/ 24765be(G1-G4,G6)/ 9e75b2b(G5)。

## 各目标落点

- **G1 OAuth 纯核泛化**:`provider::oauth` 增 `OAuthConfig`(per-provider 常量集)+ `TokenWire::{Json,Form}`;`ANTHROPIC`/`OPENAI` const;三函数收 `&OAuthConfig`。anthropic URL 回归测试锁逐字节等价前缀。
- **G2 Codex OAuth**:`login --codex` → PKCE → 本地回调 listener(127.0.0.1:1455,state 防 CSRF,`parse_callback_path` 纯核提码)→ form 交换(`HttpClient::post_form` 新接缝)→ oauth.json "openai" 键位。
- **G3 一等公民化**:`ProviderProfile.use_oauth`(serde-default);`register_oauth_profile` 登录自动登记 claude-max/chatgpt-plus 档;旧 config 零破坏(往返测试)。
- **G4 TUI**:`CODEX_OAUTH_ROW` 行;`begin_oauth`/`apply_oauth_code` 泛化;openai 流贴回调 URL 提码(TUI 零 listener 之折中)。
- **G6 /provider 热切**:oauth 徽标 + `switch_provider` oauth 档分支(bearer 热切;ponytail:同步路径不刷 token,提醒 + 启动自动刷)。
- **G5 TUI 遗留 4 bug 勘察**:
  - 底部多余状态条 / 状态条换行:**af88070 已修**(四区布局 + below_rows clamp[1,3]),用户所见为旧装版;
  - Shift+Enter 失效:**平台约束非 bug** —— Windows 不推 CSI u(WT Kitty 协议缺陷吞空格,权衡刻意);Ctrl+J 恒可用,首屏提示已注明;
  - 光标卡首行:**根因修复** —— 长首行折多视觉行时 Up 误触历史召回致缓冲突变;`up_fallback_is_home` 纯核:非行首先跳行首,行首再 Up 才召回。

## 用户插入需求(本轮)

Models 页按 provider 分栏:核实 af88070 已实现(跨 provider 收集 + 分栏标题),补行内剥前缀(1011c86);已重装二进制。

## 确定性验证

`cargo test --workspace`(217 tests)+ `cargo fmt --all --check` + `cargo clippy --workspace --all-targets -- -D warnings` 全 **exit 0**。
新增测试:authorize_url anthropic 回归 + openai 端点/scopes;parse_callback_path 四态;openai form wire(Json 路径未走);use_oauth serde 缺省+往返;login 页 CODEX 行;up_fallback_is_home 四态。

## 诚实边界

- openai client_id/端点/codex 订阅 wire 属活验证(与 iter-43 claude 同策):常量集中,`RIDGE_MODEL`/`RIDGE_BASE_URL` 可覆盖;codex 订阅或需 Responses API wire —— 用户实跑 `login --codex` 验证,不通则下一迭代最小补 wire。
- 本地回调 listener 不加单测(需真 socket;纯核 parse_callback_path 已测)。

## 开放 / 后续(iter-49 候选)

- codex wire 活验证结果回填;若需 Responses API,最小补丁。
- NLM 里程碑:iter-49 Gemini/Google One 订阅 + 代理体验;iter-50 跨订阅 maker/checker 分工(主 agent Claude Max、reviewer Codex)。
- TUI 残余(notes):虚拟化长列表 / 鼠标支持 / synchronized output。
